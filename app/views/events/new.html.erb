<%= form_for [:location, @event] do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %><br />

  <%= f.label :date %>:
  <%= f.date_select :date %><br />

  <%= f.label :requires_id %>:
  <%= f.check_box(:requires_id) %><br />


<%= f.submit %>
<% end %>
